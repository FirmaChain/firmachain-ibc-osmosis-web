import sizeDesktop from './sizeDesktop';
import urls from './urls';

const colors = {
  background: '#ffffff',
};

const theme = {
  colors,
  sizes: sizeDesktop,
  urls: urls,
};

export default theme;
